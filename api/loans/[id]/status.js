const jwt=require('jsonwebtoken'); const prisma=require('../../_lib/prisma'); module.exports=async(req,res)=>{ if(req.method!=='PATCH')return res.status(405).json({error:'Method not allowed'}); const t=(req.headers['authorization']||'').split(' ')[1]; if(!t)return res.status(401).json({error:'No token'}); try{ const u=jwt.verify(t,process.env.JWT_SECRET); if(u.role==='BORROWER')return res.status(403).json({error:'Forbidden'}); const{id}=req.query; const{status,notes}=req.body; const allowed=['APPROVED','REJECTED','DISBURSED','REPAID','DEFAULTED']; if(!allowed.includes(status))return res.status(400).json({error:'Invalid status'}); const loan=await prisma.loan.update({where:{id},data:{status,disbursedAt:status==='DISBURSED'?new Date():undefined}}); if(notes){await prisma.staffAction.create({data:{loanId:id,staffId:u.userId,action:status,notes}});} res.json(loan); }catch(e){res.status(500).json({error:e.message});} };